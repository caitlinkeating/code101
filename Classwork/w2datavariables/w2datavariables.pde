
  
  size(400,400);
  background(150);
}

  
  int numerator = 3;
  int denominator = 4; 
  
  println("3/4 = " +4/3);
  
  //it comes out as 1. integer division will only return integers
  //how do we fix this? 
  
  //casting:
  //casting changes a variable of one type to another type 
  //syntax: (type) variable 
  
  println("3/(float)4 = " + numerator/(float)denominator); 
  
  //what are more complicated data types?
  //how to make a string variable:
  
  String myString = "hello world"; 
  println(myString); 
  
  //capitalization idioms in programming
  //datatypes:
  // - simple datatypes are all lowercase (int, float, double)
  // - CLASSES: capitalize each word (String, PShape, etc.)
  
  //variables:
  // - single word is all lowercase (var1)
  // - multiple words can be camelCase (myString) 
  // - multiple words can be divided by underscores, all lowercase (my_variable)
  // - constants are all CAPS (PI)
  
  //what happens when we try to assign a number to a string? 
  
  //String strl = 4; 
  
  //how do we fix this? 
  
  String strl = str(4);
  println("strl: " +strl);
  
  //scope:
  
  void setup(){
  
    
  
  }
  
  void draw(){
  
    
  }
  
